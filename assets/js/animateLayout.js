import autoAnimate from "./index.min.js";

const postsFeed = document.querySelector("#posts-list-container");
// const uploadStatusCard = document.querySelector(
//   "#video-upload-status-container"
// );
// if (postsFeed)
//   autoAnimate(postsFeed, {
//     // Animation duration in milliseconds (default: 250)
//     duration: 350,
//     // Easing for motion (default: 'ease-in-out')
//     easing: "ease-out",
//     // When true, this will enable animations even if the user has indicated
//     // they don’t want them via prefers-reduced-motion.
//     disrespectUserMotionPreference: true,
//   });
